#include "MapInfo.h"

#include <cassert>

#include "utilities/RPGHelper.h"

namespace lcf {

// public
MapInfo::MapInfo(const uint16_t id, const std::string name) : 
        id(id), name(name) {
}


};